/*
    TODO: Getting Started | Typing Props
    ? Type Interference -> It means automatically detecting type of data
    ? use (type) when building application
    ? use (interfaces) when building application
*/